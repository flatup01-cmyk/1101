# 📝 動画の長さ制限を20秒に変更

## ✅ 変更内容

動画の長さ制限を**10秒から20秒**に変更しました。

### 変更理由

10秒以内だとエラーが頻発するため、20秒程度に調整しました。

---

## 📋 変更したファイル

### コード

1. **`functions/main.py`**
   - 動画の長さチェック: `duration > 10` → `duration > 20`
   - エラーメッセージ: "10秒以内" → "20秒以内"
   - コメント: "10秒制限" → "20秒制限"

2. **`src/main.js`**
   - 動画の長さチェック: `duration > 10` → `duration > 20`
   - エラーメッセージ: "10秒以内" → "20秒以内"

### ドキュメント

3. **`TEST_PLAN.md`**
   - テストケース: "10秒超" → "20秒超"
   - 前提条件: "10秒以内" → "20秒以内"

---

## 🚀 デプロイ状況

再デプロイを実行中です。

**デプロイコマンド**:
```bash
gcloud functions deploy process_video_trigger \
  --v2 --region=us-central1 --gen2 \
  --source=./functions \
  --entry-point=process_video_trigger \
  --trigger-event-filters='type=google.cloud.storage.object.v1.finalized' \
  --trigger-event-filters='bucket=aikaapp-584fa.firebasestorage.app' \
  --set-env-vars DIFY_API_ENDPOINT=https://api.dify.ai/v1/chat-messages,DIFY_API_KEY=app-z5S8OBIYaET8dSCdN6G63yvF \
  --memory=2GB --timeout=540s --max-instances=10
```

---

## 📊 新しい制限

| 項目 | 制限値 |
|------|--------|
| ファイルサイズ | 100MB以下 |
| 動画の長さ | **20秒以内**（変更前: 10秒以内） |
| 対応形式 | MP4, MOV, AVI, MKV |

---

## ✅ 確認方法

デプロイ完了後：

1. **LIFFアプリで動作確認**
   - 20秒以下の動画をアップロード
   - 正常に解析されることを確認
   - 20秒超の動画でエラーが出ることを確認

2. **エラーメッセージ確認**
   - "…長すぎるわよ！20秒以内に収めなさい。"

---

**最終更新**: 2025-11-03  
**ステータス**: ✅ 変更完了、再デプロイ中

